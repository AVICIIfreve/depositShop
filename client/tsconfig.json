{
  "compilerOptions": {
    // 指定目标 ECMAScript 版本
    "target": "ES2017",
    // 指定库文件
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    // 允许 JavaScript 文件
    "allowJs": true,
    // 跳过库检查
    "skipLibCheck": true,
    // 启用所有严格类型检查选项
    "strict": true,
    "noEmit": true,
    // 启用 ES 模块互操作性
    "esModuleInterop": true,
    // 指定模块系统
    "module": "esnext",
    // 指定模块解析策略
    "moduleResolution": "bundler",
    // 启用 JSON 模块解析
    "resolveJsonModule": true,
    // 启用独立模块
    "isolatedModules": true,
    /* 
    JSX 模式说明：
    - "preserve": 保留 JSX 语法，不进行转换
    - "react": 将 JSX 转换为 React.createElement 调用
    - "react-jsx": 使用 React 17+ 的 JSX 转换
    - "react-jsxdev": 开发模式的 React 17+ JSX 转换
    */
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}
